# 快速启动指南

## 🚀 一键启动所有服务

```powershell
cd E:\test2\wire2\wire
.\scripts\start_all.ps1
```

**启动的服务：**
- ✅ Redis (6379) - 数据存储
- ✅ PHP后端 (8000) - API服务器
- ✅ Go TCP服务器 (2024) - 设备通信
- ✅ WebSocket桥接 (18900) - 设备WebSocket代理

---

## 🌐 访问地址

### ✅ 推荐：已编译的管理后台（稳定版）

**本地访问：**
```
http://127.0.0.1:8000/static/admin/#/dashboard/index
```

**局域网访问：**
```
http://192.168.2.94:8000/static/admin/#/dashboard/index
```

**优点：**
- ✅ 已编译，稳定可靠
- ✅ 不需要Vite
- ✅ 所有功能完整
- ✅ 加载速度快

---

### 📱 客户交付包（设备控制面板）

**启动：**
```
双击：E:\test2\wire2\wire\static\客户交付包\启动控制面板.lnk
```

**访问：**
```
http://10.10.100.101:8080/device_direct.html
```

**特点：**
- ✅ 离线版，无需后端
- ✅ 完全隐藏后台服务
- ✅ 支持手机/平板访问
- ✅ 多设备同时访问

**停止：**
```
双击：E:\test2\wire2\wire\static\客户交付包\停止服务.bat
```

---

## ⚠️ Vite开发环境（可选）

### 当前状态
- ⚠️ Vite进程在运行但HTTP请求超时
- ⚠️ 建议使用已编译版本
- ⚠️ 仅在需要修改前端代码时使用

### 如需调试Vite
```powershell
# 停止所有Node进程
taskkill /F /IM node.exe

# 清理缓存
cd E:\test2\wire2\wire\x\admin
Remove-Item node_modules\.vite -Recurse -Force

# 启动Vite
npm run dev
```

**可能的问题：**
- 依赖版本冲突
- 构建配置错误
- 网络防火墙

---

## 📊 服务状态检查

```powershell
cd E:\test2\wire2\wire
.\scripts\status.ps1
```

---

## 🛑 停止所有服务

```powershell
cd E:\test2\wire2\wire
.\scripts\stop_all.ps1
```

---

## 💡 使用建议

### 日常使用
**推荐：** 已编译的管理后台
```
http://127.0.0.1:8000/static/admin/#/dashboard/index
```

### 客户使用
**推荐：** 客户交付包
```
双击启动 → 访问 device_direct.html
```

### 开发调试
**可选：** Vite开发环境
```
仅在需要修改前端代码时使用
```

---

## ✅ 已解决的问题

1. ✅ 客户交付包编码问题
2. ✅ 后台服务完全隐藏
3. ✅ HTTP服务器404错误
4. ✅ start_all.ps1语法错误
5. ✅ 所有后端服务正常运行

---

## 📌 常用命令

### 启动服务
```powershell
.\scripts\start_all.ps1
```

### 检查状态
```powershell
.\scripts\status.ps1
```

### 停止服务
```powershell
.\scripts\stop_all.ps1
```

### 设置静态IP
```powershell
.\scripts\设置静态IP.bat  # 右键"以管理员身份运行"
```

---

## 🎯 当前可用的访问方式

| 系统 | 地址 | 状态 |
|------|------|------|
| 管理后台（已编译） | http://127.0.0.1:8000/static/admin/#/dashboard/index | ✅ 可用 |
| 客户交付包 | http://10.10.100.101:8080/device_direct.html | ✅ 可用 |
| Vite开发环境 | http://127.0.0.1:3100 | ⚠️ 超时 |

---

**日期：** 2025-11-06  
**状态：** 所有核心服务正常运行





