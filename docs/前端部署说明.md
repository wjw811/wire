# 前端部署说明

## ⚠️ 重要：路径说明

### Web 服务器目录结构
```
E:\test2\wire2\
├── wire\                    ← PHP Web 服务器根目录
│   ├── index.php           ← 应用入口
│   ├── router.php          ← 路由文件
│   ├── static\             ← ✅ 正确的静态文件目录
│   │   └── admin\          ← 前端部署位置
│   └── x\
│       └── admin\
│           ├── src\        ← Vue 源代码
│           └── dist\       ← 构建产物（临时）
└── static\                 ← ❌ 无用目录（已删除，避免混淆）
```

### 路径对应关系

| 浏览器访问路径 | 实际文件路径 |
|--------------|-------------|
| `http://127.0.0.1:8000/static/admin/index.html` | `wire/static/admin/index.html` |
| `http://127.0.0.1:8000/static/admin/assets/xxx.js` | `wire/static/admin/assets/xxx.js` |

**关键：** PHP 的 `router.php` 使用 `__DIR__` 变量，指向 `wire/` 目录。

---

## 📦 部署流程

### 方法1：使用部署脚本（推荐）

```powershell
cd E:\test2\wire2\wire
.\scripts\deploy_frontend.ps1
```

脚本会自动：
1. 检查构建产物是否存在
2. 清空 `wire/static/admin/`
3. 从 `wire/x/admin/dist/` 复制新文件
4. 验证部署结果

### 方法2：手动部署

```powershell
# 1. 进入前端目录
cd E:\test2\wire2\wire\x\admin

# 2. 构建
npm run build

# 3. 部署到正确位置
Remove-Item ..\..\static\admin\* -Recurse -Force
Copy-Item dist\* ..\..\static\admin\ -Recurse -Force

# 4. 验证
Test-Path ..\..\static\admin\index.html
```

---

## ⚠️ 常见错误

### ❌ 错误：部署到项目根目录的 static
```powershell
# 这是错误的！
Copy-Item dist\* E:\test2\wire2\static\admin\
```

### ✅ 正确：部署到 wire/static
```powershell
# 这是正确的！
Copy-Item dist\* E:\test2\wire2\wire\static\admin\
```

---

## 🔍 验证部署

### 检查文件是否在正确位置：
```powershell
cd E:\test2\wire2\wire
Test-Path static\admin\index.html
Get-ChildItem static\admin\assets\index.*.js | Select-Object Name, Length
```

### 测试 HTTP 访问：
```powershell
Invoke-WebRequest -Uri "http://127.0.0.1:8000/static/admin/index.html" -UseBasicParsing | Select-Object StatusCode
```

应该返回 `200`。

---

## 🚀 部署后的操作

1. **清除浏览器缓存** - Ctrl + Shift + Delete
2. **强制刷新页面** - Ctrl + F5
3. **或使用无痕模式** - Ctrl + Shift + N

---

## 📝 自动刷新功能

前端已实现每 3秒 自动刷新数据：
- 代码位置：`wire/x/admin/src/views/dashboard/analysis/data.ts`
- 触发时机：页面加载 5秒后
- 刷新间隔：3秒
- 控制台日志：`[DATA.TS]` 开头的日志

后端每 3秒 主动查询设备：
- 配置文件：`wire/cmd/pomo/config.yml`
- 配置项：`tcp.timer: 3`

---

**最后更新：2025-10-31**





















